# 1. Dependencies
/node_modules
/.pnp
.pnp.js

# 2. Testing
/coverage

# 3. Next.js Build Output
/.next/
/out/
/build

# 4. Production / Misc
.DS_Store
*.pem

# 5. Debug Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# 6. Environment Variables (CRITICAL SECURITY)
# Chặn tất cả file .env chứa key bí mật
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
# Lưu ý: Không chặn .env.example (để người khác biết cần điền biến gì)

# 7. Vercel
# Folder cache của Vercel khi chạy dev
.vercel

# 8. TypeScript
*.tsbuildinfo
next-env.d.ts

# 9. Editor / IDE Settings (Optional but recommended)
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
.idea
*.swp
*.swo

# 10. Supabase (Local Dev)
# Nếu bạn dùng supabase cli local, folder này chứa config tạm
supabase/.branches
supabase/.temp